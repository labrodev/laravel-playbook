<?php

declare(strict_types=1);

namespace {{ RootNamespace }}\Domain\{{ Domain }}\Factories;

use {{ RootNamespace }}\Shared\Factories\BaseFactory;
use {{ RootNamespace }}\Domain\{{ Domain }}\Models\{{ Model }};
use Illuminate\Database\Eloquent\Factories\Factory;

/**
 * @extends Factory<{{ Model }}>
 *
 * Register on the model: #[UseFactory({{ Model }}Factory::class)]
 * (Base model must use HasFactory.)
 */
final class {{ Model }}Factory extends BaseFactory
{
    protected $model = {{ Model }}::class;

    // Public factory data definition.
    //
    // This method defines logical default values for the model
    // and may be reused in tests or seeders.
    //
    // Rules:
    // - must return plain attribute arrays
    // - may use helpers, enums, random generators
    // - must NOT trigger workflows
    // - must NOT call Actions, Services, or Jobs
    public static function factoryData(): array
    {
        return [
            // Define logical default values here.
            // Example:
            // 'status' => array_rand_value(EnumMapper::keys({{ EnumClass }}::cases())),
        ];
    }

    // Internal factory data used for persistence.
    //
    // This method adapts factoryData() to database-level fields
    // (e.g. resolving relations to *_id, *_uuid, etc.).
    //
    // Rules:
    // - must not contain business logic
    // - must not create related models implicitly
    // - must remain deterministic and side-effect free
    protected function fetchFactoryData(): array
    {
        return [
            // Map logical fields to persisted columns here.
            // Example:
            // 'status' => array_rand_value(EnumMapper::keys({{ EnumClass }}::cases())),
        ];
    }
}