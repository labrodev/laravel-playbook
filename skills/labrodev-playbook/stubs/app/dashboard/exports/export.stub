<?php

declare(strict_types=1);

namespace App\Layer\{{ Layer }}\{{ Domain }}\Exports;

use App\Layer\{{ Layer }}\{{ Domain }}\IndexQueries\{{ Model }}IndexQuery;
use Maatwebsite\Excel\Concerns\FromQuery;
use Maatwebsite\Excel\Concerns\WithHeadings;
use Maatwebsite\Excel\Concerns\WithMapping;
use {{ RootNamespace }}\Domain\{{ Domain }}\Models\{{ Model }};

/**
 * Export responsibilities:
 * - Organizes export logic for index lists.
 * - ALWAYS relies on IndexQuery as data source.
 * - Must not contain business logic.
 * - May translate values for human-readable output.
 *
 * Notes:
 * - Export is part of App-layer (presentation / IO concern).
 * - Filters and sorting are inherited from IndexQuery.
 *
 * @implements WithMapping<{{ Model }}>
 */
final readonly class {{ Model }}Export implements FromQuery, WithHeadings, WithMapping
{
    /**
     * @param  {{ Model }}IndexQuery  $query
     */
    public function __construct(
        private {{ Model }}IndexQuery $query,
    ) {}

    /**
     * Use IndexQuery as the single source of truth.
     * getEloquentBuilder() (not getQuery()) — the export must chunk hydrated
     * models so map() receives {{ Model }} instances, not stdClass rows.
     *
     * @return \Illuminate\Database\Eloquent\Builder
     */
    public function query()
    {
        return $this->query->getEloquentBuilder();
    }

    /**
     * Map model to export row.
     *
     * @param  {{ Model }}  $model
     * @return array<int, mixed>
     */
    public function map($model): array
    {
        return [
            // $model->id,
            // $model->name,
            // $model->uuid,
            // $model->status,
            // $model->created_at,
            // $model->updated_at,
        ];
    }

    /**
     * Export headings.
     *
     * @return array<int, string>
     */
    public function headings(): array
    {
        return [
            // __('ID'),
            // __('Name'),
            // __('UUID'),
            // __('Status'),
            // __('Created at'),
            // __('Updated at'),
        ];
    }
}
