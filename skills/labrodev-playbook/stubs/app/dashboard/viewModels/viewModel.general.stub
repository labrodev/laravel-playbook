<?php

declare(strict_types=1);

namespace App\Layer\{{ Layer }}\{{ Domain }}\ViewModels;

use Spatie\ViewModels\ViewModel;

final class {{ ViewModel }} extends ViewModel
{
    /**
     * ViewModel responsibilities:
     * - Collect and shape all necessary data for the template (Blade/classic MVC).
     * - Can expose helper methods for template needs (options, titles, etc.)
     * - No business logic; only presentation/data aggregation.
     *
     * Note:
     * - Unlike Inertia ViewModels, translations are not required to be fully enumerated.
     * - Methods are fully custom and depend on the template/use case.
     */
    public function __construct(
        // Inject model/DTO/query results/etc. as needed
    ) {}

    /**
     * @return array<string, mixed>
     */
    public function toArray(): array
    {
        return [
            // 'model' => $this->model,
            // 'options' => $this->options(),
        ];
    }

    /**
     * Template path for Blade/classic MVC.
     */
    public function template(): string
    {
        return '{{ blade_template }}';
    }

    // Custom methods are allowed and expected (context-specific).
    // public function title(): string { ... }
    // public function options(): array { ... }
}
